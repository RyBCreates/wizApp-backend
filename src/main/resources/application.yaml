spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/wizard_school
    username: wizard
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate        # rely on SQL scripts; change to "update" if preferred
